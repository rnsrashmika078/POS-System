export interface Food {
    image: string;
    name: string;
    quantity: number;
    available: boolean;
    price: number;
    size: string;
    category: string;
}
export interface CounterIn {
    name: string;
    count: number;
}
